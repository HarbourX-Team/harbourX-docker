# HarbourX Docker Configuration
# Copy this file to .env and adjust paths if needed

# Project root directory (relative to harbourX folder)
# Default: .. (parent directory)
# If your project structure is different, set this to the absolute path
PROJECT_ROOT=..

# Service directories (relative to PROJECT_ROOT)
# These should match your actual folder names
BACKEND_DIR=HarbourX-Backend
FRONTEND_DIR=HarbourX-Frontend
AI_MODULE_DIR=AI-Module

# HarbourX directory name (relative to PROJECT_ROOT)
# Default: harbourx (current directory name)
HARBOURX_DIR=harbourx

# Docker Compose project name
COMPOSE_PROJECT_NAME=harbourx
