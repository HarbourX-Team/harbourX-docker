# HarbourX 本地开发环境变量示例
# 复制此文件为 .env 并根据实际情况修改

# ==========================================
# PostgreSQL 数据库配置（本地 Docker）
# ==========================================
POSTGRES_DB=harbourx
POSTGRES_USER=harbourx
POSTGRES_PASSWORD=your_secure_password_here

# ==========================================
# Backend 应用配置
# ==========================================

# Spring Profiles
# - 本地开发: base,dev
# - 生产环境: prod,rds (使用 RDS 数据库)
SPRING_PROFILES_ACTIVE=base,dev

# 数据库连接（本地 Docker PostgreSQL）
DB_IP=postgres
DB_PORT=5432
DB_USER=${POSTGRES_USER:-harbourx}
DB_PASS=${POSTGRES_PASSWORD}

# 数据库连接（AWS RDS - 仅在 SPRING_PROFILES_ACTIVE=prod,rds 时使用）
# DB_RDS_ENDPOINT=your-rds-endpoint.rds.amazonaws.com
# DB_RDS_PORT=5432
# DB_RDS_DATABASE=harbourx
# DB_RDS_USERNAME=your_rds_username
# DB_RDS_PASSWORD=your_rds_password

# JWT Secret（至少 256 位，使用 openssl rand -base64 32 生成）
JWT_SECRET=your_jwt_secret_here_should_be_at_least_256_bits_long

# CORS 允许的源（逗号分隔）
FRONTEND_ALLOWED_ORIGINS=http://localhost:3001,http://localhost:80,http://frontend:80

# ==========================================
# AWS S3 配置（RCTI 文件上传功能）
# ==========================================
# AWS_S3_ACCESS=your_aws_access_key_id
# AWS_S3_SECRET=your_aws_secret_access_key

# ==========================================
# 项目目录配置（通常不需要修改）
# ==========================================
# PROJECT_ROOT=..
# BACKEND_DIR=HarbourX-Backend
# FRONTEND_DIR=HarbourX-Frontend
# AI_MODULE_DIR=AI-Module
# DOCKER_DIR=harbourX

# ==========================================
# AI-Module 环境变量
# ==========================================
# AI-Module 需要单独的 .env 文件
# 位置: ../AI-Module/.env
# 请参考 AI-Module 项目的文档配置

# ==========================================
# 注意事项
# ==========================================
# 1. 此文件仅用于本地开发环境
# 2. 生产环境部署通过 GitHub Actions CI/CD 自动完成
# 3. 生产环境的敏感信息应存储在 EC2 实例的 /opt/harbourx/.env 文件中
# 4. 不要将此文件提交到 Git 仓库（已在 .gitignore 中）
